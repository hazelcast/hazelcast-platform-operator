apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - manager.yaml
  - certificate_secret.yaml
  - webhook_service.yaml
generatorOptions:
  disableNameSuffixHash: true
images:
  - name: controller
    newName: hazelcast/hazelcast-platform-operator
    newTag: latest-snapshot
