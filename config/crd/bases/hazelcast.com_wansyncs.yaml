---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: wansyncs.hazelcast.com
spec:
  group: hazelcast.com
  names:
    kind: WanSync
    listKind: WanSyncList
    plural: wansyncs
    singular: wansync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current state of the Hazelcast WAN Sync
      jsonPath: .status.status
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WanSync is the Schema for the wansyncs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WanSyncSpec defines the desired state of WanSync
            maxProperties: 1
            minProperties: 1
            properties:
              config:
                description: Config is the configuration for the WAN replication publisher.
                  If specified the Syc operation will create a new WAN publisher.
                properties:
                  acknowledgement:
                    default:
                      timeout: 60000
                      type: ACK_ON_OPERATION_COMPLETE
                    description: Acknowledgement is the configuration for the condition
                      when the next batch of WAN events are sent.
                    properties:
                      timeout:
                        default: 60000
                        description: Timeout represents the time in milliseconds the
                          source cluster waits for the acknowledgement. After timeout,
                          the events will be resent.
                        format: int32
                        type: integer
                      type:
                        default: ACK_ON_OPERATION_COMPLETE
                        description: Type represents how a batch of replication events
                          is considered successfully replicated.
                        enum:
                        - ACK_ON_OPERATION_COMPLETE
                        - ACK_ON_RECEIPT
                        type: string
                    type: object
                  batch:
                    default:
                      maximumDelay: 1000
                      size: 500
                    description: Batch is the configuration for WAN events batch.
                    properties:
                      maximumDelay:
                        default: 1000
                        description: MaximumDelay represents the maximum delay in
                          milliseconds. If the batch size is not reached, the events
                          will be sent after the maximum delay.
                        format: int32
                        type: integer
                      size:
                        default: 500
                        description: Size represents the maximum batch size.
                        format: int32
                        type: integer
                    type: object
                  endpoints:
                    description: Endpoints is the target cluster comma separated endpoint
                      list .
                    minLength: 1
                    type: string
                  queue:
                    default:
                      capacity: 10000
                      fullBehavior: DISCARD_AFTER_MUTATION
                    description: Queue is the configuration for WAN events queue.
                    properties:
                      capacity:
                        default: 10000
                        description: Capacity is the total capacity of WAN queue.
                        format: int32
                        type: integer
                      fullBehavior:
                        default: DISCARD_AFTER_MUTATION
                        description: FullBehavior represents the behavior of the new
                          arrival when the queue is full.
                        enum:
                        - DISCARD_AFTER_MUTATION
                        - THROW_EXCEPTION
                        - THROW_EXCEPTION_ONLY_IF_REPLICATION_ACTIVE
                        type: string
                    type: object
                  resources:
                    description: Resources is the list of custom resources to which
                      WAN replication applies.
                    items:
                      properties:
                        kind:
                          default: Map
                          description: Kind is the kind of custom resource to which
                            WAN replication applies.
                          enum:
                          - Map
                          - Hazelcast
                          type: string
                        name:
                          description: Name is the name of custom resource to which
                            WAN replication applies.
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    minItems: 1
                    type: array
                  targetClusterName:
                    description: ClusterName is the clusterName field of the target
                      Hazelcast resource.
                    minLength: 1
                    type: string
                required:
                - endpoints
                - resources
                - targetClusterName
                type: object
              wanReplicationName:
                description: WanReplicationName is the name of WanReplication CR that
                  contains the WAN publisher configuration. If specified the Sync
                  operation will use existing WAN publisher.
                type: string
            type: object
          status:
            description: WanSyncStatus defines the observed state of WanSync
            properties:
              message:
                description: Message is the field to show detail information or error
                type: string
              status:
                description: Status is the status of WAN Sync
                type: string
              wanSyncMapsStatus:
                additionalProperties:
                  properties:
                    message:
                      description: Message is the field to show detail information
                        or error
                      type: string
                    phase:
                      description: Status is the status of the resource WAN sync
                      type: string
                    publisherId:
                      description: PublisherId is the ID used for WAN publisher ID
                      type: string
                    resourceName:
                      description: ResourceName is the name of the Map Custom Resource.
                      type: string
                  type: object
                description: WanSyncMapStatus is the WAN Sync status of the Maps given
                  in the spec directly or indirectly by Hazelcast resource.
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
